# The public GitHub Pages deployment does not use environment variables.
#
# Never add a real OpenAI key to this file. If a separate private backend is
# created later, configure OPENAI_API_KEY in the hosting provider's encrypted
# environment settings—not in the repository or browser code.
OPENAI_API_KEY=
